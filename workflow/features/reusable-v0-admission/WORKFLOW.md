# Reusable v0 admission

Complete the previously missing provider/application desired state and prove it
against an isolated Authentik 2026.5.2 database. No live tenant mutation is in
scope. Evidence belongs under `evidence/` after all gates pass.

# Alpha.3 library admission

Active structural and formal coding-setup admission for `pkg/authentik` and its
CLI. Completion requires the manifest acceptance gates, disposable double
import evidence, and two clean Judge reviews at one exact committed state.

schema_version = 1
project = "gotth-authentik"
profile = "strict"
active = "alpha3-library-admission"

[settings]
worktree_root = "worktrees"
workflow_root = "workflow"
default_branch = "main"

[toolchain]
build = "go build ./..."
test = "go test -mod=readonly -race -cover ./..."
lint = "go vet -mod=readonly ./..."
format = "gofmt -w"
coverage = "go test -mod=readonly -coverprofile=coverage.out ./..."

[coverage]
map = "workflow/artifacts/global-coverage-map.md"
profile = "strict"
block_unresolved_high_risk = true
exception_expiry_days = 14

[feature.extraction]
id = "extraction"
slug = "extraction"
name = "Extract generic Authentik desired-state toolkit"
namespace = "authentik"
parent = ""
state = "done"
risk = "high"
assurance = "traceable-tests"
created_at = "2026-09-02T23:36:00Z"
activated_at = "2026-09-02T23:36:00Z"
updated_at = "2026-09-02T23:50:00Z"
completed_at = "2026-09-02T23:50:00Z"
folder = "workflow/features/extraction"
worktree = "."
branch = "main"
scope = ["Extract enrollment blueprint, URL, and application-isolation contracts"]
non_goals = ["Change a live Authentik tenant", "Store Authentik credentials", "Run another service"]
affected_files = ["**"]
invariants = ["Each application has an isolated access group", "Activation requires successful email verification", "Remote mutation remains explicit"]
acceptance = ["Local validation, deterministic generation, race, vet, and coverage gates pass", "Accepted alpha.2 blueprint is retained as provenance", "Disposable Authentik import is an explicit gate before consumer release"]

[feature.extraction.rollback]
strategy = "delete_unpublished_repository"
notes = "No live Authentik state changes during extraction"
data_loss_risk = false
requires_migration_down = false

[[feature.extraction.verification]]
id = "verify"
kind = "unit"
command = "make verify"
required = true
timeout_seconds = 600
side_effect = "local_write"
network = false
retries = 0

[feature.reusable-v0-admission]
id = "reusable-v0-admission"
slug = "reusable-v0-admission"
name = "Complete reusable Authentik application boundary"
namespace = "authentik"
parent = ""
state = "done"
risk = "high"
assurance = "traceable-tests"
created_at = "2026-09-03T01:55:00Z"
activated_at = "2026-09-03T01:55:00Z"
updated_at = "2026-09-03T02:20:32Z"
completed_at = "2026-09-03T02:20:32Z"
folder = "workflow/features/reusable-v0-admission"
worktree = "/home/linus/.openclaw/workspace/.artifacts/worktrees/gotth-authentik-reusable"
branch = "feature/reusable-v0-admission"
scope = ["Create generic OAuth2 provider and application desired state", "Prove idempotent import in disposable Authentik 2026.5.2"]
non_goals = ["Change a live Authentik tenant", "Accept or emit client secrets", "Manage shared tenant flows or signing keys", "Tag a release"]
affected_files = ["README.md", "application.go", "application_test.go", "blueprint.go", "blueprint_test.go", "cmd/**", "docs/**", "examples/**", "integration/**", "workflow.toml", "workflow/**", "Makefile"]
invariants = ["Each application has an isolated provider, client ID, and access group", "Activation requires successful email verification", "Generated desired state contains no secret", "Remote mutation remains explicit"]
acceptance = ["Provider, application, enrollment, and access objects import into Authentik 2026.5.2", "Re-import preserves the generated confidential client secret", "Format, vet, race, clean-clone, coverage, and graph gates pass"]

[feature.reusable-v0-admission.rollback]
strategy = "revert_commit"
notes = "Delete the feature branch or revert its commits; disposable tenant state is destroyed after verification"
data_loss_risk = false
requires_migration_down = false

[[feature.reusable-v0-admission.verification]]
id = "verify"
kind = "unit"
command = "make verify"
required = true
timeout_seconds = 600
side_effect = "local_write"
network = false
retries = 0

[[feature.reusable-v0-admission.verification]]
id = "authentik-import"
kind = "integration"
command = "integration/verify-authentik-2026.5.2.sh"
required = true
timeout_seconds = 1200
side_effect = "external_write"
network = false
retries = 0

[feature.github-distribution]
id = "github-distribution"
slug = "github-distribution"
name = "Move public distribution identity to GitHub"
namespace = "distribution"
parent = ""
state = "done"
risk = "high"
assurance = "traceable-tests"
created_at = "2026-09-03T05:42:00Z"
activated_at = "2026-09-03T05:42:00Z"
updated_at = "2026-09-03T06:35:13Z"
completed_at = "2026-09-03T06:35:13Z"
folder = "workflow/features/github-distribution"
worktree = "/tmp/gotth-distribution-20260903/gotth-authentik"
branch = "feature/github-distribution"
scope = ["Move the Go module and repository-owned imports to github.com/gotthboard/gotth-authentik", "Document GitHub distribution and Forgejo development boundaries", "Prove clean public clone and import behavior"]
non_goals = ["Select a license", "Move or replace a tag", "Reverse the Forgejo-to-GitHub mirror", "Change repository ownership or account type"]
affected_files = ["README.md", "CONTRIBUTING.md", "SECURITY.md", "go.mod", "*.go", "cmd/**", "docs/**", "workflow.toml", "workflow/features/github-distribution/**"]
invariants = ["Forgejo remains canonical development", "GitHub receives refs only through the existing push mirror", "Runtime and public API behavior remain unchanged", "Existing tags remain immutable"]
acceptance = ["The exact old module path is absent outside canonical Forgejo URLs", "Format, tidy, vet, test, clean-clone, public import, mirror parity, and two cold Judge passes succeed", "License and account decisions remain explicitly blocked"]

[[feature.github-distribution.dependency]]
id = "reusable-v0-admission"
type = "prerequisite"
state = "satisfied"
evidence = "workflow/features/reusable-v0-admission/evidence/verification.md"
satisfied_at = "2026-09-03T05:42:00Z"
source_commit = "96178fe0a76a7d80c21dd15243a1f1701bd323d0"

[feature.github-distribution.rollback]
strategy = "revert_feature_commit"
notes = "Revert the migration commit; never rewrite existing tags or mirror history"
data_loss_risk = false
requires_migration_down = false

[[feature.github-distribution.interface]]
name = "Go module import path"
kind = "api"
stability = "unstable-unreleased"
change = "breaking-module-identity"

[[feature.github-distribution.verification]]
id = "local"
kind = "unit"
command = "go mod tidy, go vet -mod=readonly ./..., and go test -mod=readonly ./..."
required = true
timeout_seconds = 1200
side_effect = "local_write"
network = false
retries = 0

[[feature.github-distribution.verification]]
id = "public-import"
kind = "integration"
command = "clean clone and import from github.com/gotthboard/gotth-authentik"
required = true
timeout_seconds = 1200
side_effect = "external_read"
network = true
retries = 0

[[feature.github-distribution.verification]]
id = "mirror-parity"
kind = "integration"
command = "compare exact Forgejo and GitHub advertised heads and tags"
required = true
timeout_seconds = 300
side_effect = "external_read"
network = true
retries = 0

[feature.alpha3-library-admission]
id = "alpha3-library-admission"
slug = "alpha3-library-admission"
name = "Formally admit the structured alpha.3 library"
namespace = "authentik"
parent = ""
state = "in_progress"
risk = "critical"
assurance = "traceable-tests"
created_at = "2026-09-03T06:04:51Z"
activated_at = "2026-09-03T06:04:51Z"
updated_at = "2026-09-03T06:04:51Z"
folder = "workflow/features/alpha3-library-admission"
source_path = "pkg/authentik"
worktree = "/tmp/gotth-authentik-coding-setup"
branch = "feature/openclaw-coding-setup-admission"
scope = ["Single canonical public-package layout", "CLI canonical import", "Governance-only module root", "Coding-setup traceability, runtime, performance, review, and evidence"]
non_goals = ["Apply a blueprint, handle credentials or secrets, or mutate a live tenant", "Tag before consumer admission"]
affected_files = ["*.go", "pkg/authentik/**", "cmd/**", "README.md", "docs/**", "workflow.toml", "workflow/**"]
invariants = ["Desired state remains deterministic and secret-free", "Remote mutation remains explicit", "Disposable double-import remains the completeness oracle"]
acceptance = ["Canonical consumer, disposable Authentik import, format, vet, race, clean-clone, graph, and two independent Judge gates pass"]

[[feature.alpha3-library-admission.dependency]]
id = "github-distribution"
type = "prerequisite"
state = "satisfied"
evidence = "workflow/features/github-distribution/evidence/verification.md"
source_commit = "0279e0f127f9bb79f95d5bf41e3df1e5066a431e"
satisfied_at = "2026-09-03T06:30:00Z"

[feature.alpha3-library-admission.rollback]
strategy = "revert_admission_commit"
notes = "No tag or consumer pin exists; revert the structural admission commit"
data_loss_risk = false
requires_migration_down = false

[[feature.alpha3-library-admission.verification]]
id = "verify"
kind = "unit-integration"
command = "make verify plus disposable Authentik 2026.5.2 double import"
required = true
timeout_seconds = 1800
side_effect = "external_write"
network = false
retries = 0

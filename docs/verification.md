# Verification

`make verify` checks format, vet, race tests, coverage, and generation of the
GOTTH Board example. The alpha.2 provenance blueprint was previously admitted
by the Authentik 2026.5.2 importer. A newly generated blueprint still requires
disposable-tenant importer verification before a consumer pins a release.
